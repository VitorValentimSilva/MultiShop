## npm audit

The reported high severity vulnerability comes from a transitive
dependency (`hono`) used internally by the Prisma CLI.

- Affects development tooling only
- Not bundled in production
- No runtime impact

Action: accepted, awaiting upstream fix.

/**
 * * Number of milliseconds in a single day.
 * * Used for date arithmetic and range calculations.
 * ! This value assumes a fixed 24h day (does not account for DST changes).
 */
export const MS_PER_DAY = 86_400_000;
